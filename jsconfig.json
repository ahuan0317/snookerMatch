// 可进行类型检查
{ 
  // 编译选项
  "compilerOptions": {
     // 解析非相关模块名称的基础目录
    "baseUrl": "./",
    "paths": {
        // 指定相对于baseUrl选项计算的路径映射，使用webpack别名，智能感知路径
        "@/*": ["src/*"]
    }
  },
  // 要排除的文件
  "exclude": ["node_modules", "dist"]
}